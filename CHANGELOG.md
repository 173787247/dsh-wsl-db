# Changelog

## 0.1.1

- `db_ping`: read-only connectivity probe (`SELECT 1` / Redis `PING`).
- Richer `db_status` JSON formatting for aliases / redisHosts.

## 0.1.0

- Read-only `db_status`, `db_psql`, `db_redis`.
